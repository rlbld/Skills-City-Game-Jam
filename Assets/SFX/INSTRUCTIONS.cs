using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class INSTRUCTIONS : MonoBehaviour
{
    // Hello team! Here are instructions on how this works quickly, Increase the "Max Distance" on the Audio Source under "3D Sound Settings" to increase or decrease range
    // To change what audio is being played drag the sound you want from the "SFX" folder and replace the AudioClip with it in the Audio Source


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
